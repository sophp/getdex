#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../@module-federation/cli/bin/mf.js" "$@"
else
  exec node  "$basedir/../@module-federation/cli/bin/mf.js" "$@"
fi
